# Environment

The development environment is managed by pixi and is called `py314`.  Use `pixi s -e py314` to open a shell in the correct environment.  The pixi environment is defined in `~/git/torch-build`.  Pixi commands need to be run from there.  Do not make changes to that directory.
